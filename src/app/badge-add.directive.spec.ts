import { BadgeAddDirective } from './badge-add.directive';

describe('BadgeAddDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeAddDirective();
    expect(directive).toBeTruthy();
  });
});
