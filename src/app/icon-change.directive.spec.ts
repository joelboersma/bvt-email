import { IconChangeDirective } from './icon-change.directive';

describe('IconChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new IconChangeDirective();
    expect(directive).toBeTruthy();
  });
});
